function greaterNumber(arg1, arg2) {
    let numA = Number(arg1);
    let numB = Number(arg2);
    if (numA > numB) {
        console.log(numA);
    } else {
        console.log(numB);
    }
}
greaterNumber(3, 3);